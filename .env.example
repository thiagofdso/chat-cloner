# Exemplo de configuração para Clonechat
CLONER_DELAY_SECONDS=2
CLONER_DOWNLOAD_PATH=./data/downloads/

# ========================================
# CONFIGURAÇÃO DO TELEGRAM API
# ========================================
# Obtenha estas credenciais em: https://my.telegram.org/apps
# São obrigatórias para o funcionamento do sistema
TELEGRAM_API_ID=InsiraSeuApiIdAqui
TELEGRAM_API_HASH=InsiraSeuApiHashAqui

# ========================================
# CONFIGURAÇÃO DO CLONER (FUNCIONALIDADE EXISTENTE)
# ========================================
# Delay entre operações de clonagem (em segundos)
CLONER_DELAY_SECONDS=2
# Pasta onde arquivos baixados serão salvos
CLONER_DOWNLOAD_PATH=./data/downloads/

# ========================================
# CONFIGURAÇÃO DO PIPELINE ZIMATISE
# ========================================
# Limite de tamanho para arquivos (em MB)
# Arquivos maiores que este valor serão divididos
FILE_SIZE_LIMIT_MB=1000

# Modo de compactação para documentos
# Opções: "zip" (recomendado) ou "rar"
MODE=zip

# Extensões de vídeo suportadas pelo sistema
# Vídeos com estas extensões serão processados
VIDEO_EXTENSIONS=mp4,avi,webm,ts,vob,mov,mkv,wmv,3gp,flv,ogv,ogg,rrc,gifv,mng,qt,yuv,rm,asf,amv,m4p,m4v,mpg,mp2,mpeg,mpe,mpv,svi,3g2,mxf,roq,nsv,f4v,f4p,f4a,f4b

# Plano de recodificação de vídeos
# "single" = processa cada vídeo individualmente
# "group" = junta vídeos em grupos
REENCODE_PLAN=single

# Limite de duração para vídeos (formato HH:MM:SS.mm)
# Vídeos mais longos serão divididos
DURATION_LIMIT=02:00:00.00

# Ativar transições entre vídeos juntados
# Opções: "true" ou "false"
ACTIVATE_TRANSITION=false

# Índice inicial para numeração de arquivos
# Começa a contar a partir deste número
START_INDEX=1

# Prefixo para hashtags de vídeos
# Exemplo: "F" gera #F01, #F02, #F03...
HASHTAG_INDEX=F

# Adaptar descrições automaticamente
# Opções: "true" ou "false"
DESCRIPTIONS_AUTO_ADAPT=true

# Modo silencioso (reduz logs)
# Opções: "true" ou "false"
SILENT_MODE=true

# Arquivo de cabeçalho do sumário
# Conteúdo que aparece no topo do sumário
PATH_SUMMARY_TOP=summary_top.txt

# Arquivo de rodapé do sumário
# Conteúdo que aparece no final do sumário
PATH_SUMMARY_BOT=summary_bot.txt

# Prefixo para hashtags de documentos
# Exemplo: "Materiais" gera #Materiais001, #Materiais002...
DOCUMENT_HASHTAG=Materiais

# Título da seção de documentos no sumário
# Aparece como cabeçalho da seção de documentos
DOCUMENT_TITLE=Materiais

# Registrar link de convite do canal criado
# Opções: "1" (sim) ou "0" (não)
REGISTER_INVITE_LINK=1

# ========================================
# CONFIGURAÇÕES ADICIONAIS DO ZIMATISE
# ========================================
# Comprimento máximo de caminhos de arquivo
# Importante para sistemas Windows
MAX_PATH=260

# Criar novo canal automaticamente
# Opções: "1" (sim) ou "0" (não)
CREATE_NEW_CHANNEL=1

# ID do canal de destino (se não criar novo)
# Formato: -100xxxxxxxxx
CHAT_ID=-100111111111

# ID do canal MOC (Message of the Channel)
# Formato: -100xxxxxxxxx
MOC_CHAT_ID=-10022222222

# Deletar vídeos temporários automaticamente
# Opções: "1" (sim) ou "0" (não)
AUTODEL_VIDEO_TEMP=1

# Lista de administradores do canal
# Separados por vírgula (ex: "user1,user2")
CHANNEL_ADMS=

# Limite de tempo para operações (em minutos)
TIME_LIMIT=99

# Enviar MOC (Message of the Channel)
# Opções: "1" (sim) ou "0" (não)
SEND_MOC=0

# Mover arquivos para pasta "uploaded" após upload
# Opções: "1" (sim) ou "0" (não)
MOVE_TO_UPLOADED=1